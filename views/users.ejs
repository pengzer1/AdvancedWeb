<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/users.css">   
    <script src="https://kit.fontawesome.com/a5f3739fb3.js" crossorigin="anonymous"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1/jquery.min.js"></script>
    <script src="https://d1p7wdleee1q2z.cloudfront.net/post/search.min.js"></script>
    <script src="http://code.jquery.com/jquery-latest.min.js"></script>
    <title>Neighbor</title>
</head>

<body>
    <% include header %>
    <div class="display2">
        <p id="newProd">근처 유저</p>
        <div class="items">
            <% if (length === 0) { %>
                <div>유저 없음</div>
                <% } else { 
                    for(var i = 0; i < length && i < 8; i++) {%>
                <a><div><div class="item_div"><img src="<%= rows[i].image %>">
                    <ul>
                        <li><div class="text_div"><%=rows[i].birth%></div></li>
                        <li><div class="text_div"><%=rows[i].intro%></div></li>
                    </ul>
                    </div> 
                    <%if(rows[i].name == session.name){%>
                        <button id="tog<%=i%>" value="unvis"
                            style="width: 250px; border: 0; outline: 0; font-size: 15px; background-color: white;">
                            <%=rows[i].name%>
                        </button>
                        <%}else{%>
                        <button id="tog<%=i%>" value="unvis"
                            onclick="
                            if(this.value == 'unvis'){
                                this.nextSibling.nextSibling.style.display='block';
                                this.value = 'vis';
                            }
                            else {
                                this.nextSibling.nextSibling.style.display='none'; 
                                this.value='unvis';
                            }"
                            style="width: 250px; border: 0; outline: 0; font-size: 15px; background-color: white; cursor: pointer;">
                            <%=rows[i].name%>
                        </button>
                        <%}%>
                    <ul id="1" style="display: none;">
                        <li id="2">
                            <button onclick="location.href='/smsPopup/<%=rows[i].name%>'" style="width: 250px; border: 0; outline: 0; font-size: 15px; background-color: white; cursor: pointer;">
                                쪽지 보내기
                            </button>
                        </li>
                    </ul></div></a>
                <% } } %>
            </div>
        </div>

 <% include footer %>
</body>
</html>